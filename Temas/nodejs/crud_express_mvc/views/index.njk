{% extends "layout.njk" %}
{% block title %}
    <title>{{ title }}</title>
{% endblock %}
{% block content %}
    <h2>{{ title }}</h2>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Nombre</th>
                <th>Descripción</th>
                <th>Estado</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            {% for tarea in dbTareas %}
                <tr>
                    <td>{{ tarea.id }}</td>
                    <td>{{ tarea.nombre }}</td>
                    <td>{{ tarea.descripcion }}</td>
                    <td class="{% if tarea.completa %}completa{% else %}incompleta{% endif %}">
                        {% if tarea.completa %}Completa{% else %}Incompleta{% endif %}
                    </td>
                    <td>
                        <a href="/edit-task/{{ tarea.id }}">Editar la tarea {{ tarea.nombre }}</a>
                    </td>
                                        <td>
                        <a href="/complete-task/{{ tarea.id }}">Completar la tarea {{ tarea.nombre }}</a>
                    </td>

                    <td>
                        <a href="/delete-task/{{ tarea.id }}">Eliminar la tarea {{ tarea.nombre }}</a>
                    </td>
                    
                </tr>
            {% endfor %}
        </tbody>
    </table>
    <div>
        <a href="/add-task">Agregar tarea</a>
    </div>
{% endblock %}